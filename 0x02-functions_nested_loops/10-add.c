#include "main.h"
/**
 * add - program to add two number
 *
 * @x: first integer
 *
 * @y: second integer
 *
 * Return: Always 0 (success)
 */
int add(int x, int y)
{
	int a;

	a = x + y;
	return (a);
}
